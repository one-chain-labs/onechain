[package]
name = "example"
edition = "2024.beta" # edition = "legacy" to use legacy (pre-2024) Move

[dependencies]
# One = { local = "FRAMEWORK_DIR" }
dependency = { local = "../dependency" }

[addresses]
example = "0x0"
